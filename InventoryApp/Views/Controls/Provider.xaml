<UserControl x:Class="InventoryApp.Views.Controls.Provider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryApp.ViewModels.User"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:ProviderViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid Margin="0,180,0,0">
            <DataGrid HorizontalAlignment="Left" Style="{StaticResource DataGridStyle}" ItemsSource="{Binding ProviderModels}" SelectedItem="{Binding SelectedItem}">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Name}" Header="Name"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Surname}" Header="Surname"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Adress}" Header="Adress"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Email}" Header="E-Mail"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Company}" Header="Company"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
