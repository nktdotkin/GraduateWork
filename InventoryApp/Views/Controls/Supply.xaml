<UserControl x:Class="InventoryApp.Views.Controls.Supply"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryApp.ViewModels.Product"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:SupplyViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid Margin="0,180,0,0">
            <DataGrid HorizontalAlignment="Left" Style="{StaticResource DataGridStyle}" ItemsSource="{Binding SupplyModels}">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Date}" Header="Date"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Amount}" Header="Amount"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Product}" Header="Product"/>
                    <DataGridTextColumn Width="100" Binding="{Binding Path=Provider}" Header="Provider"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
